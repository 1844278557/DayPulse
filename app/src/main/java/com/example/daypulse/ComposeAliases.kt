package com.example.daypulse

/**
 * Receiver alias used by the settings-card slot in DayPulseActivity.
 * It resolves to Compose's ColumnScope so children can use weight/alignment modifiers.
 */
typealias Column = androidx.compose.foundation.layout.ColumnScope
